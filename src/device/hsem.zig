// HSEM
pub const HSEM = struct {
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R0: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R1: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R2: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R3: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R4: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R5: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R6: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R7: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R8: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R9: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R10: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R11: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R12: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R13: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R14: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R15: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R16: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R17: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R18: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R19: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R20: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R21: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R22: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R23: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R24: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R25: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R26: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R27: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R28: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R29: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R30: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM register HSEM_R0 HSEM_R31
    HSEM_R31: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR0: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR1: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR2: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR3: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR4: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR5: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR6: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR7: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR8: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR9: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR10: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR11: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR12: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR13: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR14: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR15: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR16: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR17: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR18: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR19: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR20: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR21: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR22: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR23: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR24: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR25: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR26: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR27: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR28: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR29: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR30: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Read lock register
    HSEM_RLR31: packed struct(u32) {
        // Semaphore ProcessID
        PROCID: u8,
        // Semaphore MasterID
        MASTERID: u8,
        // Reserved
        _reserved_16: u15,
        // Lock indication
        LOCK: u1,
    },
    // HSEM Interrupt enable register
    HSEM_IER: packed struct(u32) {
        // Interrupt semaphore n enable bit
        ISEM0: u1,
        // Interrupt semaphore n enable bit
        ISEM1: u1,
        // Interrupt semaphore n enable bit
        ISEM2: u1,
        // Interrupt semaphore n enable bit
        ISEM3: u1,
        // Interrupt semaphore n enable bit
        ISEM4: u1,
        // Interrupt semaphore n enable bit
        ISEM5: u1,
        // Interrupt semaphore n enable bit
        ISEM6: u1,
        // Interrupt semaphore n enable bit
        ISEM7: u1,
        // Interrupt semaphore n enable bit
        ISEM8: u1,
        // Interrupt semaphore n enable bit
        ISEM9: u1,
        // Interrupt semaphore n enable bit
        ISEM10: u1,
        // Interrupt semaphore n enable bit
        ISEM11: u1,
        // Interrupt semaphore n enable bit
        ISEM12: u1,
        // Interrupt semaphore n enable bit
        ISEM13: u1,
        // Interrupt semaphore n enable bit
        ISEM14: u1,
        // Interrupt semaphore n enable bit
        ISEM15: u1,
        // Interrupt semaphore n enable bit
        ISEM16: u1,
        // Interrupt semaphore n enable bit
        ISEM17: u1,
        // Interrupt semaphore n enable bit
        ISEM18: u1,
        // Interrupt semaphore n enable bit
        ISEM19: u1,
        // Interrupt semaphore n enable bit
        ISEM20: u1,
        // Interrupt semaphore n enable bit
        ISEM21: u1,
        // Interrupt semaphore n enable bit
        ISEM22: u1,
        // Interrupt semaphore n enable bit
        ISEM23: u1,
        // Interrupt semaphore n enable bit
        ISEM24: u1,
        // Interrupt semaphore n enable bit
        ISEM25: u1,
        // Interrupt semaphore n enable bit
        ISEM26: u1,
        // Interrupt semaphore n enable bit
        ISEM27: u1,
        // Interrupt semaphore n enable bit
        ISEM28: u1,
        // Interrupt semaphore n enable bit
        ISEM29: u1,
        // Interrupt semaphore n enable bit
        ISEM30: u1,
        // Interrupt(N) semaphore n enable bit.
        ISEM31: u1,
    },
    // HSEM Interrupt clear register
    HSEM_ICR: packed struct(u32) {
        // Interrupt(N) semaphore n clear bit
        ISEM0: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM1: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM2: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM3: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM4: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM5: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM6: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM7: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM8: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM9: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM10: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM11: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM12: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM13: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM14: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM15: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM16: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM17: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM18: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM19: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM20: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM21: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM22: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM23: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM24: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM25: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM26: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM27: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM28: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM29: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM30: u1,
        // Interrupt(N) semaphore n clear bit
        ISEM31: u1,
    },
    // HSEM Interrupt status register
    HSEM_ISR: packed struct(u32) {
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM0: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM1: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM2: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM3: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM4: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM5: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM6: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM7: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM8: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM9: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM10: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM11: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM12: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM13: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM14: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM15: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM16: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM17: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM18: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM19: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM20: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM21: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM22: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM23: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM24: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM25: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM26: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM27: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM28: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM29: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM30: u1,
        // Interrupt(N) semaphore n status bit before enable (mask)
        ISEM31: u1,
    },
    // HSEM Masked interrupt status register
    HSEM_MISR: packed struct(u32) {
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM0: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM1: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM2: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM3: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM4: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM5: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM6: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM7: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM8: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM9: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM10: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM11: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM12: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM13: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM14: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM15: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM16: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM17: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM18: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM19: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM20: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM21: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM22: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM23: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM24: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM25: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM26: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM27: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM28: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM29: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM30: u1,
        // masked interrupt(N) semaphore n status bit after enable (mask)
        ISEM31: u1,
    },
    // HSEM Clear register
    HSEM_CR: packed struct(u32) {
        // Reserved
        _reserved_0: u8,
        // MasterID of semaphores to be cleared
        MASTERID: u8,
        // Semaphore clear Key
        KEY: u16,
    },
    // HSEM Interrupt clear register
    HSEM_KEYR: packed struct(u32) {
        // Reserved
        _reserved_0: u16,
        // Semaphore Clear Key
        KEY: u16,
    },
};

pub const HSEM_BASE_ADDRESS: usize = 0x58026400;
pub const HSEM_REGISTERS: *volatile HSEM = @ptrFromInt(HSEM_BASE_ADDRESS);
